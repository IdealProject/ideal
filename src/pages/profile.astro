---
import Layout from "@layouts/Layout.astro";
import { Image } from "astro:assets";
const { email, name, picture } = Astro.locals;
---
<Layout title="Perfil de Editor">
  <section class="py-24 px-4 w-full flex flex-col items-center gap-4">
    <div class="overflow-hidden rounded-full border-2 border-indigo-100 shadow-sm">
      <Image
        src={picture}
        alt={`Foto de Perfil de ${name}`}
        height={120}
        width={120}
        class="object-cover" 
      />
    </div>
    <div class="text-center">
      <p class="text-xl font-medium text-zinc-900">{name}</p>
      <p class="text-sm text-zinc-500">{email}</p>
    </div>
    <p class="max-w-md text-center text-sm text-zinc-600">
      Esta es una página protegida. Solo los usuarios autenticados pueden
      acceder a esta sección.
    </p>
    <a
      href="/api/auth/signout"
      class="mt-2 px-6 py-2 text-sm font-medium text-zinc-100 bg-zinc-900 rounded-full transition hover:bg-zinc-800"
    >
      Cerrar Sesión
    </a>
  </section>
</Layout>
